defmodule Journey.Insights.FlowAnalyticsTest do
  @moduledoc """
  Placeholder module for Journey.Insights.FlowAnalytics tests.

  Tests have been split into specialized modules for better parallel execution:
  - Journey.Insights.FlowAnalyticsCoreTest - Core functionality tests
  - Journey.Insights.FlowAnalyticsValidationTest - Input validation tests
  """

  use ExUnit.Case, async: true

  test "placeholder test" do
    # This is a placeholder to keep the module valid.
    # Actual tests are in FlowAnalyticsCoreTest and FlowAnalyticsValidationTest.
    assert true
  end
end
